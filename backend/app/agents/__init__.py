"""Agentic Orchestration Architecture (CLAUDE.md §11A).

Each agent wraps existing, already-correct services rather than reimplementing them — this
package is an orchestration layer, not a rewrite of the ACIF/RAG/GraphRAG pipeline.
"""
